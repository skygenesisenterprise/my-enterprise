---
title: Deployments
description: Publier une nouvelle version de service.
---

Un deployment represente une version immutable d'un service.

```bash
sge deploy --project api --environment production
```

## Rollback

Le rollback repointe le trafic vers un deployment precedent sans reconstruire l'image.
